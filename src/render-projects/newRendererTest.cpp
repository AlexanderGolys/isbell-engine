#include "isbell.hpp"

using namespace glm;
using std::vector, std::string, std::shared_ptr, std::unique_ptr, std::pair, std::make_unique, std::make_shared, std::cout, std::endl;


int main() {
	WindowSettings winSettings = WindowSettings("window", FHD);
	AppSettings appSettings = AppSettings(winSettings);
	App app(appSettings);


}
